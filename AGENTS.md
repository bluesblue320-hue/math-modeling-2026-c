# AGENTS.md — 多 Agent 协作的唯一事实源与硬约束

本仓库由多个 Agent 协作完成 **2026 高教社杯数学建模 C 题**（微网与外部电网电力调控策略）。
本文件规定各 Agent 必须遵守的 **Source of Truth（唯一事实源）优先级** 与 **不可自行修改的 invariants**。
**任何 Agent（A2/A3/A4…）在开始工作前必须先读本文件。**

---

## 1. Source of Truth（唯一事实源）与优先级

三个文件各司其职，**不得交叉维护同一内容、不得各自保留不同版本的同一规则**。

| 优先级 | 文件 | 职责（它是什么的 Source of Truth） | 谁负责维护 |
| --- | --- | --- | --- |
| 1 | `docs/problem_spec.md` | **题目事实与未决事项**：输入/输出、参数、单位、明确约束、官方模板结构、待澄清风险项。凡「题目到底说了什么」一律以此为准。 | A1 Spec Agent |
| 2 | `docs/time_mapping_decision.md` | **时间映射**：`source_label ↔ model_period t ↔ physical_interval ↔ output_interval/row` 的唯一映射与状态（DECIDED/PENDING）。 | A1（修订）/ 人工确认 |
| 3 | `docs/model_spec.md` | **数学模型**：目标函数、约束、变量、假设、LP/MILP 判定、求解与验证要求。 | A1 Model Agent |

规则：

- 下游 Agent 引用规则时，必须引用**对应职责文件**，不得凭记忆或自行推断。
- 若三个文件之间出现**任何冲突**（含时间口径、参数、约束、目标），相关 Agent **必须立即停止并报告**，**禁止**自行选择其中一个继续执行。
- 修改任一 Source of Truth 文件，必须留痕（说明改了什么、依据什么），并同步检查其余两个文件是否受影响。

**当前阻断状态（务必先读）**：

```
docs/time_mapping_decision.md → STATUS: PENDING ; BLOCK_A2: TRUE
```

即时间映射**尚未可靠确定**。在解除 `BLOCK_A2` 之前，**禁止 A2 对数据做任何正式转换或结果回填**（详见下节）。

---

## 2. 不可自行修改的核心定义（Invariants）

以下定义已被 `model_spec.md` / `problem_spec.md` 固定，**任何 Agent 不得根据自己的理解静默修改、重命名或重新解释**：

### 2.1 粒度与单位

- 时间步长 **$\Delta t = 1/6$ h（= 10 min）**，不得改为其他步长。
- 一天 **144 个时段**（$t = 1,\dots,144$），145 个状态边界。
- **功率统一用 kW**（负载、光伏、购电、充放电）。
- **电量统一用 kWh**（购电量、充放电量、储电量、购电费相关量）。
- **储能状态统一记作 $E_t$**（单位 kWh，时段 $t$ 结束时电量）。
- **禁止把 1200~10800 直接称为 SOC。** 若需无量纲荷电比，须另行定义 $\text{SOC}_t = E_t / 12000$；主模型统一使用 $E_t$。

### 2.2 目标与模型类型

- **主目标：最小化外网购电费用**，即 $\min \sum_t c_t P^{\text{grid}}_t \Delta t$。
- **问题 1 主模型：不加电池退化成本**，保持 **LP**（不引入 0-1 模式变量）。
- **禁止擅自加入**：售电收益、紧急购电、5 倍电价、违约费用、调整购电，以及**任何问题 2/3/4 的机制**。
- 充放电互斥**不作为**主模型的硬约束；但**禁止**再写「LP 自动保证充放电互斥」「同时充放电绝不出现在最优解」这类绝对表述；求解后**必须**做同时充放电独立验证（见 `model_spec.md` 第 7、9 节）。

### 2.3 光伏与平衡

- **允许弃光**：$0 \le P^{\text{pv,use}}_t \le P^{\text{PV}}_t$，多余光伏不强制消纳。
- **功率平衡取等式**：$P^{\text{grid}}_t + P^{\text{pv,use}}_t + P^{\text{dis}}_t = P^{\text{load}}_t + P^{\text{ch}}_t$。
- **禁止售电**：$P^{\text{grid}}_t \ge 0$。

### 2.4 时间映射（最敏感）

- **禁止任何 Agent 根据自己的理解静默修改时间映射。**
- 输入标签、模型时段、result1.xlsx 输出行的对应关系，**只以 `docs/time_mapping_decision.md` 为准**。
- 当前该文件为 `PENDING`，即**不得**自行判定「标签=区间起点/终点」，**不得**静默平移模板（"整体平移一格"也不行）。

---

## 3. 待确认的唯一阻断问题（Blocker）

在 `docs/time_mapping_decision.md` 由 `PENDING` 变为 `DECIDED` 之前：

- A2 数据 Agent **不得**解析重排附件 1 时间列、**不得**对齐或回填 result1.xlsx。
- 唯一需要人工确认的问题：

  > 附件 1 的时间标签（00:10 … 24:00）是区间的**结束时刻**还是**开始时刻**？
  > 即：论文表 1 的「10:00–10:10」应取附件 1 **第 62 行**（标签 `10:10`）还是 **第 61 行**（标签 `10:00`）？

- 确认后：在 `time_mapping_decision.md` 写入 `STATUS: DECIDED`、`BLOCK_A2: FALSE`，并只保留一份生效映射（另一候选标注「已否决」）。此后 A2 方可正式开工。

---

## 4. 协作边界（Scope）

- 本轮（A1 修订）**只**负责问题 1 的 `problem_spec.md` / `model_spec.md` / `time_mapping_decision.md` 与构建本文件；**不**做 A2 数据清洗、**不**写优化代码、**不**运行 Solver、**不**生成 result1.xlsx、**不**处理问题 2/3/4。
- 下游 Agent **不得**扩展问题 1 模型范围；如需新增假设/约束，必须先更新对应 Source of Truth 并说明依据，不得直接改代码/结果。
- 发现数学错误可修正，但须在对应 Source of Truth 中记录；例外：题目明示的约束（平衡、效率、容量、首尾相等）不得擅自更改。

---

*本文件为多 Agent 协作的入口约束。若与其他文档冲突，先停止并报告，不要自行取舍。*
