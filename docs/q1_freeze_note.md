# Q1 Freeze Note — 问题 1 主模型冻结记录

> 本文件记录问题 1（确定性条件下微网日前优化调度）主模型的冻结状态。
> 冻结后**只允许论文表达、可视化与结果展示层修改**，不得再改动主模型本身。

## STATUS: FROZEN

```
MODEL FROZEN
DELIVERY READY
PAPER ASSETS READY
```

---

## 1. 主模型版本

| 项目 | 内容 |
| --- | --- |
| 模型 | 问题 1 确定性日前调度 **LP**（离散 144 时段，无 0-1 变量） |
| 数学模型事实源 | `docs/model_spec.md` |
| 时间映射事实源 | `docs/time_mapping_decision.md`（Mapping A） |
| 题目事实源 | `docs/problem_spec.md` |
| 建模实现 | `src/model_q1.py`（`build_model`） |
| 求解/验证实现 | `src/solve_q1.py` |
| 数据 | `data/processed/q1_data.csv`（A2 冻结产物） |

## 2. 主结果（FROZEN，不得漂移）

| 指标 | 数值（元 / kWh） |
| --- | ---: |
| **主 objective（全天购电费用）** | **35126.948589289634** 元 |
| 无储能 baseline 购电费用 | 48052.046590846665 元 |
| 节省金额 | 12925.098001557031 元 |
| 节省比例 | 26.8981217629 % |
| 全天购电量 | 59482.69899835391 kWh |
| 储能充电量 / 放电量 | 20740.666131687245 / 16799.939566666668 kWh |
| 储电量 min / max | 1200 / 10800 kWh |
| 首尾储电量 E0 / E144 | 6000 / 6000 kWh |
| 弃光量 | 0 kWh |
| 同时充放电计数 | 0 |

任何后续修改若使主 objective 超出数值误差（≤ 1e-6 元）发生变化，**必须立即停止并报告**。

## 3. 关键假设（均为建模解释，非题目明文）

- **效率假设**：`η_ch = η_dis = 0.90`（H3 主模型解释），往返效率 `0.81`。
  题目只写“充放电效率 90%”，单程/往返存在歧义；敏感性分析 Scenario B 采用
  `η_ch = η_dis = √0.90`（往返 0.90）作对照，**不改变主模型**。
- **时间映射**：采用 **Mapping A**（附件 1 时间标签 = 对应 10 min 区间的**结束时刻**）。
  该映射是**建模解释**（为完整覆盖当天 00:00–24:00 并与首尾储电量边界、表 2 边界一致），
  **不是题目明文定义**；详见 `docs/time_mapping_decision.md`。
- 允许弃光；不考虑售电；问题 1 主模型**不加入**电池退化成本。

## 4. 验证与交付状态

| 项目 | 状态 |
| --- | --- |
| A3 内部物理验证（平衡 / 递推 / 边界 / 首尾 / 费用复算） | PASS |
| CBC 交叉校验（独立求解器） | Optimal，与主目标一致（相对差 ≈ 5.5e-11） |
| **A4 Independent LP Validation** | **PASS**（独立 SciPy LP 重构，diff = 0） |
| 效率敏感性分析（Scenario A/B） | 已完成（B −3.7733%，定性结论一致） |
| result1 导出（双版本） | 已完成、通过校验 |
| 论文表 1 / 表 2 / 指标 JSON / 图 | 已生成（`outputs/paper/`） |
| 回归测试 | 全部通过 |

## 5. 冻结范围（后续允许 / 禁止）

**允许**：论文文字表达、图表样式、结果展示层调整、`outputs/paper/` 素材再生成。

**禁止**（除非发现明确数学错误并先更新 Source of Truth）：

- 新 Solver、新 Agent、新框架、新优化算法；
- MILP、电池退化成本、售电收益、紧急购电、违约费用等机制；
- 改动 Mapping A、`Δt = 1/6 h`、`N_PERIODS = 144`、主效率 `0.90`、
  LP 目标函数、储能容量与上下限、充放电功率上限、当前主方案与主 objective；
- 任何 architecture refactor。

---

*本文件为主模型冻结状态的正式记录；如与三个 Source of Truth 冲突，以 Source of Truth 为准并立即上报。*
